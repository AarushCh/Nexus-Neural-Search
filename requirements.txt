# --- Web / API ---
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.9
python-dotenv==1.0.1

# --- Auth / DB ---
sqlalchemy>=2.0.37   # 2.0.25 breaks on Python 3.13 (TypingOnly assertion)
psycopg2-binary      # Postgres driver — used when DATABASE_URL points at a hosted DB
passlib[bcrypt]==1.7.4
bcrypt==3.2.0
python-jose[cryptography]

# --- Vector DB (Qdrant Cloud) --- server is 1.19.x; client capped at 1.16.x (newest that
# supports Python 3.9, which Render runs). Version-mismatch warning is expected and harmless.
qdrant-client>=1.16.0,<1.17.0

# --- Local embeddings (ONNX, no external inference API) ---
# fastembed runs dense (bge-small) + sparse (bm25) on onnxruntime — no torch, fits Render's
# 512MB free tier.
fastembed>=0.3.6
# Optional cross-encoder rerank (ENABLE_RERANK=true). Heavy (~600MB) — install only on a host
# with spare RAM, NOT Render free:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install sentence-transformers>=2.7.0

# --- Grounded RAG (OpenRouter / Nemotron) ---
openai

# --- Data / ingest ---
pandas
requests==2.31.0
