# --- Web / API ---
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.9
python-dotenv==1.0.1

# --- Auth / DB ---
sqlalchemy>=2.0.37   # 2.0.25 breaks on Python 3.13 (TypingOnly assertion)
psycopg2-binary      # Postgres driver — used when DATABASE_URL points at a hosted DB
passlib[bcrypt]==1.7.4
bcrypt==3.2.0
python-jose[cryptography]

# --- Vector DB (Qdrant Cloud) --- server is 1.18.x, keep client within one minor
qdrant-client>=1.14.0

# --- Local embeddings + reranking (no external inference API) ---
# torch CPU wheel: install with
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch
sentence-transformers>=2.7.0
fastembed>=0.3.6

# --- Grounded RAG (OpenRouter / Nemotron) ---
openai

# --- Data / ingest ---
pandas
requests==2.31.0
