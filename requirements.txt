# --- Web / API ---
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.9
python-dotenv==1.0.1

# --- Auth / DB ---
# Postgres holds everything now: users, wishlist, history AND the catalogue
# (pgvector for the dense index, tsvector for the lexical one).
sqlalchemy>=2.0.37   # 2.0.25 breaks on Python 3.13 (TypingOnly assertion)
psycopg2-binary
passlib[bcrypt]==1.7.4
bcrypt==3.2.0
python-jose[cryptography]

# --- Local inference (ONNX, no torch) ---
# fastembed runs BOTH the dense encoder (bge-small) and the cross-encoder
# reranker on onnxruntime. Reranking previously needed torch + sentence-
# transformers (~600MB) and had to be disabled in production; on ONNX it fits
# a small instance, so it is on by default.
# 0.4+ is required for fastembed.rerank.cross_encoder.
fastembed>=0.4

# --- Grounded RAG (OpenRouter) ---
openai

# --- Catalogue build ---
requests==2.31.0
